import axios from 'axios'\n\nconst API_URL = 'http://localhost:5000/api'\n\nconst getAuthHeader = () => ({\n  headers: {\n    Authorization: `Bearer ${localStorage.getItem('token')}`\n  }\n})\n\n// Auth APIs\nexport const signup = (username, email, phoneNumber, password) => {\n  return axios.post(`${API_URL}/auth/signup`, {\n    username,\n    email,\n    phoneNumber,\n    password\n  })\n}\n\nexport const login = (email, phoneNumber, password) => {\n  return axios.post(`${API_URL}/auth/login`, {\n    email,\n    phoneNumber,\n    password\n  })\n}\n\n// User APIs\nexport const getUser = (userId) => {\n  return axios.get(`${API_URL}/users/${userId}`, getAuthHeader())\n}\n\nexport const followUser = (userId) => {\n  return axios.post(`${API_URL}/users/${userId}/follow`, {}, getAuthHeader())\n}\n\n// Video APIs\nexport const getVideoFeed = () => {\n  return axios.get(`${API_URL}/videos/feed`, getAuthHeader())\n}\n\nexport const likeVideo = (videoId) => {\n  return axios.post(`${API_URL}/videos/${videoId}/like`, {}, getAuthHeader())\n}\n\nexport const commentOnVideo = (videoId, text) => {\n  return axios.post(`${API_URL}/videos/${videoId}/comments`, { text }, getAuthHeader())\n}\n\n// Stream APIs\nexport const startStream = (title, description) => {\n  return axios.post(`${API_URL}/streams/start`, { title, description }, getAuthHeader())\n}\n\nexport const getLiveStreams = () => {\n  return axios.get(`${API_URL}/streams/live`, getAuthHeader())\n}\n\nexport const stopStream = () => {\n  return axios.post(`${API_URL}/streams/stop`, {}, getAuthHeader())\n}\n\n// Message APIs\nexport const getConversations = () => {\n  return axios.get(`${API_URL}/messages/conversations`, getAuthHeader())\n}\n\nexport const sendMessage = (conversationId, text) => {\n  return axios.post(`${API_URL}/messages/${conversationId}`, { text }, getAuthHeader())\n}\n